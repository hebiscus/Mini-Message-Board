extends layout

block content
  div
    each message in messages
      div
        h5= message.user 
        p= message.text 
        p= message.added
    a(href="/new")
      button= 'make a new message'
